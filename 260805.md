### 문항1. Numpy 배열로 5명 학생의 4과목 성적표를 만들고 축(axis) 기준 통계를 산출하시오.


아래 1차원 리스트는 학생 5명이 4개 과목(국어, 영어, 수학, 과학 순)에서 받은 점수를 이어 붙인 것입니다.

Numpy만 사용하여(반복문 사용 금지) 다음을 순서대로 수행하고 결과를 출력하세요.

```py
scores = [88, 92, 79, 95,
          75, 68, 82, 70,
          92, 99, 95, 88,
          60, 72, 65, 58,
          85, 80, 78, 90]
```

* 위 리스트를 5행 4열 ndarray로 변환하고 shape, ndim, size, dtype을 출력하시오.

* 학생 별 평균과 과목 별 평균을 각각 구하시오.

* 전체에서 가장 높은 점수와 그 점수가 몇 번째 학생·몇 번째 과목인지 구하시오.

* 전체 평균, 표준편차, 분산을 구하시오.

* 평균이 80점 이상인 학생의 인덱스와 인원수를 구하고, 해당 학생들의 성적만 잘라내어 출력하시오.

* 90점 이상인 점수의 개수, 100점 이상인 점수가 하나라도 있는지, 모든 점수가 60점 이상인지를 판별하시오.
 
* 90점 이상은 'A', 80점 이상은 'B', 나머지는 'C'로 변환한 등급 배열을 만드시오.

### 정답
```py
import numpy as np

scores = [88, 92, 79, 95,
          75, 68, 82, 70,
          92, 99, 95, 88,
          60, 72, 65, 58,
          85, 80, 78, 90]
arr = np.array(scores).reshape(5, 4)

print(arr)

print("shape:", arr.shape)
print("ndim:", arr.ndim)
print("size:", arr.size)
print("dtype:", arr.dtype)

student_avg = arr.mean(axis=1)
subject_avg = arr.mean(axis=0)

print("학생별 평균:", student_avg)
print("과목별 평균:", subject_avg)

print("최고 점수:", arr.max())
print("위치(학생, 과목):", np.unravel_index(arr.argmax(), arr.shape))

print("전체 평균:", arr.mean())
print("표준편차:", arr.std())
print("분산:", arr.var())

idx80 = np.where(student_avg >= 80)[0]
selected = arr[idx80]

print("80점 이상 학생 인덱스:", idx80)
print("인원수:", idx80.size)
print("해당 학생 성적:")
print(selected)

count90 = np.sum(arr >= 90)
any100 = np.any(arr >= 100)
all60 = np.all(arr >= 60)

print("90점 이상 개수:", count90)
print("100점 이상 존재:", any100)
print("모든 점수 60점 이상:", all60)

grade = np.where(arr >= 90, "A",
         np.where(arr >= 80, "B", "C"))

print("등급 배열:")
print(grade)
```

### 문항2. 8x8 난수 배열을 흑백 이미지로 가정하고 슬라이싱·이진화·좌우 반전을 구현하시오.

0~255 사이의 정수 난수로 이루어진 8행 8열 배열을 흑백 이미지 데이터라고 가정합니다.  
채점 결과를 동일하게 맞추기 위해 반드시 아래 시드를 먼저 실행하세요.

```py
import numpy as np
np.random.seed(2024)
img = np.random.randint(0, 256, size=(8, 8))
```
* 생성한 img를 출력하시오.

* 이미지 정중앙의 4×4 영역만 잘라내어 출력하시오.

* 짝수 번째 행(0, 2, 4, 6행)만 뽑아내시오.

* 128 이상인 픽셀은 255(흰색), 미만인 픽셀은 0(검은색)으로 바꾼 이진화 배열을 만드시오.

* 이진화 결과에서 흰색 픽셀과 검은색 픽셀의 개수를 각각 세시오.

* 원본 이미지를 좌·우 절반으로 분할하시오.

* 분할한 두 조각의 순서를 바꿔 다시 이어 붙여 좌우가 뒤바뀐 이미지를 만드시오.

* 원본 이미지를 1차원으로 펼친 뒤 앞 10개 값과 shape을 출력하시오.

* 128 이상인 픽셀만 골라내어 그 개수와 평균값을 구하시오.

### 정답
```py
import numpy as np

np.random.seed(2024)
img = np.random.randint(0, 256, size=(8, 8))

print(img) 

print("\n정중앙 4x4 영역")
print(img[2:6, 2:6])

print("\n짝수 번째 행")
print(img[::2])

binary = np.where(img >= 128, 255, 0)
print("\n이진화 배열")
print(binary)

print("\n흰색 픽셀 개수:", np.sum(binary == 255))
print("검은색 픽셀 개수:", np.sum(binary == 0))

print("\n왼쪽 절반")
print(img[:, :4])
print("\n오른쪽 절반")
print(img[:, 4:])

print("\n좌우 반전 이미지")
print(np.concat((right, left), axis=1))

flat = img.ravel()
print("\n앞 10개 값:", flat[:10])
print("shape:", flat.shape)

pixels128 = img[img >= 128]
print("\n128 이상 픽셀 개수:", pixels128.size)
print("128 이상 픽셀 평균:", pixels128.mean())
```

### 문항3. 지점별 판매 데이터에 브로드캐스트와 행렬 연산을 적용해 매출을 분석하시오.

3개 지점이 4개 제품을 판매한 수량과, 제품별 단가가 다음과 같습니다.

```py
sales = np.array([[120,  80,  45,  60],     # A지점
                  [200, 150,  90,  30],     # B지점
                  [ 75,  60, 120,  95]])    # C지점
price = np.array([1500, 3000, 5000, 2000])  # 제품 1~4 단가
```

### 정답
```py
import numpy as np

sales = np.array([[120,  80,  45,  60],     # A지점
                  [200, 150,  90,  30],     # B지점
                  [ 75,  60, 120,  95]])    # C지점

price = np.array([1500, 3000, 5000, 2000])

revenue = sales * price

print("제품별 매출액")
print(revenue)

print("\n지점별 총매출")
print(revenue.sum(axis=1))

print("\n제품별 총매출")
print(revenue.sum(axis=0))

print("\n전체 총매출")
print(revenue.sum())

print("\n행렬 연산 결과")
print(sales @ price)

print("\n가장 매출이 높은 지점 인덱스:", np.argmax(branch_total))
print("최고 지점 매출:", revenue.sum(axis=1)[best_branch])

best_product = np.argmax(sales.sum(axis=0))
print("\n가장 많이 팔린 제품 인덱스:", best_product)
print("판매 수량:", sales.sum(axis=0)[best_product])

print("\n지점 평균 매출:", branch_total.mean())
print("제품 평균 매출:", product_total.mean())
```
