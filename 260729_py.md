## 문항1. 재귀 함수
> 재귀 함수를 사용했을 때 얻을 수 있는 장점과 단점을 반복문과 비교하여 작성하시오.

### 정답
```py
# 재귀 함수
def fib(n):
    if n == 1 or n == 2: # n in [1, 2]
        return 1
    return fib(n-1) + fib(n-2)
fib(10)

# 반복문
def fib_loop(n):
    if n == 1 or n == 2:
        return 1
    a = b = 1

    for _ in range(n-2):
        # x, y = y, x
        b, a = a + b, b

    return b
fib_loop(10)

# 재귀 함수는 코드가 간결하고 문제를 자연스럽게 표현할 수 있지만, 호출 스택을 사용하여 메모리 사용량이 증가한다. 반복문은 메모리 사용이 적고 실행 속도가 빠르다.
```



## 문항2. Circle 클래스 생성, 활용
> 다음의 요구사항을 만족하는 Circle 클래스를 정의하고, 넓이와 둘레, 중심 좌표를 구하는 메서드를 정의하시오.

```py
# 반지름: 3, x좌표: 2, y좌표: 4
c1 = Circle(3, 2, 4)

c1.area() #=> 28.26
c1.circumference() #=> 18.84
c1.center() #=> (2, 4)
```

### 정답
```py
class Circle:
    def __init__(self, r, x, y):
        self.r = r
        self.x = x
        self.y = y

    def area(self):
        area = 3.14 * self.r * self.r
        return area

    def circumference(self):
        length = 2 * 3.14 * self.r
        return length

    def center(self):
        return (self.x, self.y)


c1 = Circle(3, 2, 4)

print(c1.area())      
print(c1.circumference())
print(c1.center())       
```