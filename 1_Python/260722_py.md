## 문항1. 근의 공식 Python 코드
> 파이썬을 활용하여 근의 공식 수식을 코드로 표현하기

### 정답
```py
a = float(input("a를 입력하세요: "))
b = float(input("b를 입력하세요: "))
c = float(input("c를 입력하세요: "))

x1 = (-b + (b**2 - 4*a*c)**0.5) / (2*a)
x2 = (-b - (b**2 - 4*a*c)**0.5) / (2*a)

print("x1:", x1)
print("x2:", x2)
```